server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # This block handles React Router's client-side routing
  location / {
    try_files $uri $uri/ /index.html;
  }
}